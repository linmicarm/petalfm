Drop a `preview.png` here — a night + day screenshot side by side. It's
referenced at the top of the root README. Run `npm run dev`, screenshot both
themes, and combine them into one image named preview.png.
